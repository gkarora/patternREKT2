function [lambda] = parametric_exponential( a,b )
    function [lambda] = get_lambda (data)
        summation = sum(data(:));
        num = length(data);
        x_bar = summation / num;

        lambda = 1/x_bar;
    end


lambda_a = get_lambda(a);
lambda_b = get_lambda(b);

x = 0:0.1:5;
est_y_a_ex = exppdf(x, lambda_a);
est_y_b_ex = exppdf(x, lambda_b);
act_y_ex = exppdf(x, 1);
figure(201);
plot(x, y_ex, 'g');
plot(x, y_b_ex, 'r');

figure(x, y_a_ex, 'g')
end

